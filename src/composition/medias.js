import {ref} from "vue";
export function medias() {
    return {
        medias: ref([
            /* reactive({
                 pic: null,
                 url: null,
                 alt: null,
                 bullet: true
             }),*/
        ])
    }
}
